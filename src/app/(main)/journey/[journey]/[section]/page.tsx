export default function JourneySection() {
  // Share blog slug to client component
  // let blogDetails = await getBlog(params.blog);

  // If no blog is found, return 404
  // if (!blogDetails?.posts || blogDetails.posts.length === 0) {
  //   return NotFound(); // This will trigger the 404 page
  // }

  return <div className="text-inherit">Section</div>;
}
